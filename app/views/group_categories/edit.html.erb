<h1>Editing group_category</h1>

<% form_for(@group_category) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @group_category %> |
<%= link_to 'Back', group_categories_path %>