<% title "Groups" %>

<% for group in @groups %>
<p>
<h1><%= link_to group.name, group_path(group) %></h1>
(<%= t('group.founder') %><%= ": " %><%= link_to group.owner.login, user_path(group.owner) %>)
</p>
	
<% end %>


<p><%= link_to t('common.new'), new_group_path %></p>
