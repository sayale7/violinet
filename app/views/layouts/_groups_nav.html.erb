<ul>
	<li>
		<%= link_to t("common.my_groups"), mygroups_path %>
	</li>
	<li>
		<%= link_to t('group.new'), new_group_path %>
	</li>
	<% if can? :manage, Tag %>
	<li>
		<%= link_to t('group.manage_tags'), '/tags?taggable_type=Group' %>
	</li>
	<% end %>
</ul>